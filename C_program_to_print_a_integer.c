#include <stdio.h>

main()
{
    int number;

    printf("Enter the number");

    //reads and stores the input given by the user

    scanf("%d", &number);

    //display
    printf("You have entered %d",number);
}
